#/bin/bash
git clone --bare git@github.com:TheCaav/MyHome.git .cfg
function config {
   /usr/bin/git --git-dir=$PWD/.cfg/ --work-tree=$PWD $@
}

config checkout
config checkout
if [ $? = 0 ]; then
  echo "Checked out config.";
  else
    echo "Backing up pre-existing dot files.";
    config checkout 2>&1 | egrep "\s+\." | awk {'print $1'} | xargs -I{} mv {} .config-backup/{}
fi;
config checkout
config submodule init
config submodule update
config config status.showUntrackedFiles no
config submodule update --remote
echo "The following alias should be added:"
echo "alias config='/usr/bin/git --git-dir="""$PWD"""/.cfg/ --work-tree="""$PWD"""/ $@'"

echo "cloning vimix themes"
git clone https://github.com/vinceliuice/vimix-gtk-themes.git vimix-gtk
git clone https://github.com/vinceliuice/vimix-icon-theme.git vimix-icons
echo "installing themes"
cd vimix-gtk
sh install.sh
cd ..
cd vimix-icons
sh install.sh
cd ..
echo "removing git folders"
rm -rf vimix-gtk
rm -rf vimix-icons
